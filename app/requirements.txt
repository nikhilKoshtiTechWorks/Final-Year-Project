# debug is set to 1 i.e. on for development envrionment (to see the change by reloading the webpage) by command 'set FLASK_DEBUG=1' just change it to '0' when going for production
blinker==1.7.0
click==8.1.7
colorama==0.4.6
Flask==3.0.0
itsdangerous==2.1.2
Jinja2==3.1.2
MarkupSafe==2.1.3
Werkzeug==3.0.1
